#' Run the Migraine Risk Shiny App
#'
#' @export
run_app <- function() {
  app_dir <- system.file("app", package = "migrane")
  shiny::runApp(app_dir)
}
#the app has an error warning
#Problem in logic  here, the app does not ask for femlae cycle info and this creates an input error
#also the logic of the wine question does not work, it still displays both questions at the same time
